

/*Tablet*/

@media (min-width: 767px) {
  body {
    background-color: var(--blue);
  }

  .menu {
    & header {
      display: flex;
      justify-content: space-between;
      & nav {
        & ul {
          flex-direction: row;
        }
      }
    }
  }

}
