/*Tablet*/

@media (min-width: 767px) {
  .menu {
    & header {
      & .logo {
        position: absolute;
        margin-left: 25px;
          &::after {
            content: none;
          }
      }
      & .burger-menu {
        display: none;
      }
      & nav {
        position: static;
        border-radius: 0;
        width: auto;
        height: auto;
        padding: 15px;
        justify-content: flex-end;
        & ul {
          flex-direction: row;
          align-items: center;
          padding: 0;
        }
      }
    }
  }

  .our-beginning {
    & .get-started {
      flex-direction: row;
      flex-wrap: wrap;

      & .get-started_image {
        & img {
          //width: auto;
        }
      }
      & .get-started_description {
        margin: 25px;
      }
    }

  }

  .features {
    & .feature-descriptions {
      flex-direction: row;

    }

  }

  .testimonials {
    & .user-testimonials {
      flex-direction: row;
      flex-wrap: wrap;
      & .user-card {
      margin: 25px;
        & .avatar {
          & img {
            top: -18%;
            left: 90px;
          }
        }

      }
    }
  }

}
